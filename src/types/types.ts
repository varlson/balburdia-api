export type ExpensesType = {
  author: string;
  desc: string;
  value: string;
  date: string;
};

export type MonthStatusType = {
  currentMonth: string;
  totalOfMonth: string;
  totalExpenses: string;
  balance: string;
};
